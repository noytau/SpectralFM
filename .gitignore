# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
venv/
.venv/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
*.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.vscode/

# IDEs
.idea/
.DS_Store

# Evaluation outputs (large generated artifacts)
code/eval_results/

# Pulled RunAI outputs (kubectl cp); keep local only
artifacts/

# Logs and data
*.log
*.sqlite3
data/
data-bin/
checkpoints/
dump/
multirun/
outputs/
wandb/

# Large files (generic patterns - adapt as needed)
# PyTorch checkpoints
*.pt
*.bin # Binary files
*.tar
*.gz
*.zip
*.h5
*.hdf5
*.onnx
*.safetensors

# Large files (explicitly ignored after removal from Git history)
fairseq/base_libri.pt
fairseq/audio_base_ls.pt
fairseq/base_libri_960h.pt

# Reproducible embedding-feature caches (regenerate via eval.label_probe;
# multi-GB, not a result — never commit)
code/eval_outputs/label_probe/*/bank.npz

# Exploratory label_probe runs (e.g. eval.runner --evals label_probe sweeps
# over external label sets) and their merged input trees (thousands of
# symlinks + a bank.npz per set) — reproducible from the source data, never
# committed. The curated original study's outputs under
# code/eval_outputs/label_probe/run_*/ are unaffected by this and stay
# tracked as before.
code/eval_outputs/label_probe_inputs/
code/eval_outputs/label_probe_regression/
code/eval_outputs/label_probe_regression_merged/
code/eval_outputs/label_probe_smoke/

# Local working notes (rolling-status / daily-log skills) — never commit
rolling_status.md
rolling_status.html
